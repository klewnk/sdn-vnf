server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    location / {
        try_files /index.html =404;
    }

    location /slow {
        default_type text/plain;
        limit_rate 256;
        try_files /slow.txt =404;
    }
}
